{
  "mcpServers": {
    "appium-mcp": {
      "type": "stdio",
      "command": "npx",
      "args": ["appium-mcp@latest"],
      "env": {
        "AI_VISION_ENABLED": "false",
        "APPIUM_MCP_ON_CLIENT_DISCONNECT": "delete_all"
      }
    }
  }
}
